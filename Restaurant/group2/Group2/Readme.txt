Instructions:

	The schema restman.sql is provided to you along with the setup. Run XAMPP selecting Apache server and MySQL. Connect to localhost/phpMyAdmin in your browser using:

Data source: localhost
Username: root
Password: group2

Import the database restman.sql into it. Now you can install the software. Before any of the next runs of the software, you just need to run XAMPP selecting Apache server and MySQL. There is no need to open the browser.
